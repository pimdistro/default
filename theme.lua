-- this just returns a simple lazy-nvim plugin, feel free to set this up the way you want.
return {
  "B4mbus/oxocarbon-lua.nvim",
  config = function()
    vim.cmd.colorscheme("oxocarbon-lua")
  end,
}
